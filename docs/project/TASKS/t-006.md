# T-006: Basic UI Component Library

**Task ID**: T-006  
**Priority**: P0 (MVP Critical)  
**Estimated Time**: 3-4 days  
**Dependencies**: T-002 (디자인 시스템 구축), T-004 (TypeScript 타입), T-005 (인증 시스템)  
**Phase**: 1 (기반 구축)

## 📋 Task Overview

TailwindCSS 4와 database_schema_v4.1의 멀티테넌트 아키텍처를 기반으로 한 재사용 가능한 UI 컴포넌트 라이브러리를 구축합니다. 접근성(WCAG 2.1 AA), 성능(60fps), 그리고 ClassFlow 드래그앤드롭 시스템에 최적화된 컴포넌트들을 제공합니다.

## 🎯 Success Criteria

- [ ] 25+ 개의 재사용 가능한 UI 컴포넌트
- [ ] WCAG 2.1 AA 접근성 표준 100% 준수
- [ ] TypeScript 완전 지원 (Generic types)
- [ ] Storybook 문서화 완료
- [ ] 권한 기반 조건부 렌더링 지원
- [ ] ClassFlow 특화 컴포넌트 (드래그앤드롭)
- [ ] 반응형 디자인 완전 지원
- [ ] 60fps 성능 보장

## 📚 Technical Requirements

### 1. Core Components Architecture

#### 컴포넌트 구조
```typescript
// src/components/ui/types.ts
export interface BaseComponentProps {
  className?: string
  children?: React.ReactNode
  'data-testid'?: string
}

export interface InteractiveProps {
  disabled?: boolean
  loading?: boolean
  onClick?: () => void
}

export interface AccessibilityProps {
  'aria-label'?: string
  'aria-describedby'?: string
  'aria-expanded'?: boolean
  role?: string
}

export type ComponentVariant = 'primary' | 'secondary' | 'success' | 'warning' | 'error' | 'ghost'
export type ComponentSize = 'xs' | 'sm' | 'md' | 'lg' | 'xl'
```

### 2. Form Components

#### Button 컴포넌트
```typescript
// src/components/ui/Button.tsx
'use client'

import React, { forwardRef } from 'react'
import { cn } from '@/utils/cn'
import { usePermissions } from '@/contexts/AuthContext'
import type { BaseComponentProps, InteractiveProps, AccessibilityProps, ComponentVariant, ComponentSize } from './types'

interface ButtonProps extends 
  BaseComponentProps, 
  InteractiveProps, 
  AccessibilityProps,
  React.ButtonHTMLAttributes<HTMLButtonElement> {
  variant?: ComponentVariant
  size?: ComponentSize
  fullWidth?: boolean
  leftIcon?: React.ReactNode
  rightIcon?: React.ReactNode
  permission?: { resource: string; action: string }
}

export const Button = forwardRef<HTMLButtonElement, ButtonProps>(({
  className,
  variant = 'primary',
  size = 'md',
  disabled = false,
  loading = false,
  fullWidth = false,
  leftIcon,
  rightIcon,
  children,
  permission,
  'data-testid': testId,
  'aria-label': ariaLabel,
  ...props
}, ref) => {
  const { hasPermission } = usePermissions()

  // 권한 체크
  if (permission && !hasPermission(permission.resource, permission.action)) {
    return null
  }

  const baseClasses = 'btn'
  const variantClasses = {
    primary: 'btn-primary',
    secondary: 'btn-secondary',
    success: 'btn-success',
    warning: 'btn-warning',
    error: 'btn-error',
    ghost: 'btn-ghost'
  }
  
  const sizeClasses = {
    xs: 'px-2 py-1 text-xs',
    sm: 'px-3 py-1.5 text-sm',
    md: 'px-4 py-2 text-sm',
    lg: 'px-6 py-3 text-base',
    xl: 'px-8 py-4 text-lg'
  }

  const isDisabled = disabled || loading

  return (
    <button
      ref={ref}
      className={cn(
        baseClasses,
        variantClasses[variant],
        sizeClasses[size],
        fullWidth && 'w-full',
        isDisabled && 'opacity-50 cursor-not-allowed',
        className
      )}
      disabled={isDisabled}
      aria-label={ariaLabel}
      data-testid={testId}
      {...props}
    >
      {loading && (
        <span className="loading-spinner mr-2" aria-hidden="true" />
      )}
      {leftIcon && !loading && (
        <span className="mr-2" aria-hidden="true">{leftIcon}</span>
      )}
      {children}
      {rightIcon && (
        <span className="ml-2" aria-hidden="true">{rightIcon}</span>
      )}
    </button>
  )
})

Button.displayName = 'Button'
```

#### Input 컴포넌트
```typescript
// src/components/ui/Input.tsx
'use client'

import React, { forwardRef, useState } from 'react'
import { cn } from '@/utils/cn'
import type { BaseComponentProps, AccessibilityProps, ComponentSize } from './types'

interface InputProps extends 
  BaseComponentProps, 
  AccessibilityProps,
  Omit<React.InputHTMLAttributes<HTMLInputElement>, 'size'> {
  size?: ComponentSize
  error?: string
  hint?: string
  leftIcon?: React.ReactNode
  rightIcon?: React.ReactNode
  label?: string
  required?: boolean
}

export const Input = forwardRef<HTMLInputElement, InputProps>(({
  className,
  size = 'md',
  error,
  hint,
  leftIcon,
  rightIcon,
  label,
  required = false,
  'data-testid': testId,
  'aria-label': ariaLabel,
  'aria-describedby': ariaDescribedBy,
  id,
  ...props
}, ref) => {
  const [isFocused, setIsFocused] = useState(false)
  
  const inputId = id || `input-${Math.random().toString(36).slice(2)}`
  const errorId = error ? `${inputId}-error` : undefined
  const hintId = hint ? `${inputId}-hint` : undefined
  const describedBy = [ariaDescribedBy, errorId, hintId].filter(Boolean).join(' ')

  const baseClasses = 'input'
  const sizeClasses = {
    xs: 'px-2 py-1 text-xs',
    sm: 'px-3 py-1.5 text-sm',
    md: 'px-3 py-2 text-sm',
    lg: 'px-4 py-3 text-base',
    xl: 'px-5 py-4 text-lg'
  }

  return (
    <div className="space-y-1">
      {label && (
        <label 
          htmlFor={inputId}
          className={cn(
            'block text-sm font-medium text-secondary-700',
            required && 'after:content-["*"] after:ml-0.5 after:text-error-500'
          )}
        >
          {label}
        </label>
      )}
      
      <div className="relative">
        {leftIcon && (
          <div className="absolute inset-y-0 left-0 pl-3 flex items-center pointer-events-none">
            <span className="text-secondary-400" aria-hidden="true">
              {leftIcon}
            </span>
          </div>
        )}
        
        <input
          ref={ref}
          id={inputId}
          className={cn(
            baseClasses,
            sizeClasses[size],
            leftIcon && 'pl-10',
            rightIcon && 'pr-10',
            error && 'input-error',
            isFocused && 'ring-2',
            className
          )}
          aria-label={ariaLabel}
          aria-describedby={describedBy || undefined}
          aria-invalid={error ? 'true' : 'false'}
          data-testid={testId}
          onFocus={() => setIsFocused(true)}
          onBlur={() => setIsFocused(false)}
          {...props}
        />
        
        {rightIcon && (
          <div className="absolute inset-y-0 right-0 pr-3 flex items-center pointer-events-none">
            <span className="text-secondary-400" aria-hidden="true">
              {rightIcon}
            </span>
          </div>
        )}
      </div>
      
      {hint && !error && (
        <p id={hintId} className="text-sm text-secondary-500">
          {hint}
        </p>
      )}
      
      {error && (
        <p id={errorId} className="text-sm text-error-600" role="alert">
          {error}
        </p>
      )}
    </div>
  )
})

Input.displayName = 'Input'
```

### 3. Layout Components

#### Card 컴포넌트
```typescript
// src/components/ui/Card.tsx
'use client'

import React from 'react'
import { cn } from '@/utils/cn'
import type { BaseComponentProps } from './types'

interface CardProps extends BaseComponentProps {
  padding?: 'none' | 'sm' | 'md' | 'lg'
  shadow?: 'none' | 'sm' | 'md' | 'lg'
  border?: boolean
  hover?: boolean
}

export function Card({ 
  className, 
  children, 
  padding = 'md',
  shadow = 'sm',
  border = true,
  hover = false,
  'data-testid': testId
}: CardProps) {
  const paddingClasses = {
    none: '',
    sm: 'p-4',
    md: 'p-6',
    lg: 'p-8'
  }
  
  const shadowClasses = {
    none: '',
    sm: 'shadow-sm',
    md: 'shadow',
    lg: 'shadow-lg'
  }

  return (
    <div 
      className={cn(
        'card',
        paddingClasses[padding],
        shadowClasses[shadow],
        !border && 'border-0',
        hover && 'hover:shadow-md transition-shadow duration-200',
        className
      )}
      data-testid={testId}
    >
      {children}
    </div>
  )
}

export function CardHeader({ className, children, ...props }: BaseComponentProps) {
  return (
    <div className={cn('card-header', className)} {...props}>
      {children}
    </div>
  )
}

export function CardBody({ className, children, ...props }: BaseComponentProps) {
  return (
    <div className={cn('card-body', className)} {...props}>
      {children}
    </div>
  )
}

export function CardFooter({ className, children, ...props }: BaseComponentProps) {
  return (
    <div className={cn('card-footer', className)} {...props}>
      {children}
    </div>
  )
}
```

#### Modal 컴포넌트
```typescript
// src/components/ui/Modal.tsx
'use client'

import React, { useEffect, useRef } from 'react'
import { createPortal } from 'react-dom'
import { cn } from '@/utils/cn'
import { Button } from './Button'
import type { BaseComponentProps } from './types'

interface ModalProps extends BaseComponentProps {
  isOpen: boolean
  onClose: () => void
  title?: string
  size?: 'sm' | 'md' | 'lg' | 'xl' | 'full'
  closable?: boolean
  footer?: React.ReactNode
}

export function Modal({ 
  isOpen, 
  onClose, 
  title, 
  size = 'md',
  closable = true,
  footer,
  className, 
  children,
  'data-testid': testId
}: ModalProps) {
  const overlayRef = useRef<HTMLDivElement>(null)
  const modalRef = useRef<HTMLDivElement>(null)

  useEffect(() => {
    if (isOpen) {
      document.body.style.overflow = 'hidden'
      
      // 포커스 트래핑을 위해 첫 번째 포커스 가능한 요소에 포커스
      const focusableElement = modalRef.current?.querySelector(
        'button, [href], input, select, textarea, [tabindex]:not([tabindex="-1"])'
      ) as HTMLElement
      
      focusableElement?.focus()
      
      return () => {
        document.body.style.overflow = ''
      }
    }
  }, [isOpen])

  useEffect(() => {
    function handleEscape(event: KeyboardEvent) {
      if (event.key === 'Escape' && closable) {
        onClose()
      }
    }

    if (isOpen) {
      document.addEventListener('keydown', handleEscape)
      return () => document.removeEventListener('keydown', handleEscape)
    }
  }, [isOpen, closable, onClose])

  if (!isOpen) return null

  const sizeClasses = {
    sm: 'max-w-sm',
    md: 'max-w-md',
    lg: 'max-w-lg',
    xl: 'max-w-2xl',
    full: 'max-w-full h-full'
  }

  function handleOverlayClick(event: React.MouseEvent) {
    if (event.target === overlayRef.current && closable) {
      onClose()
    }
  }

  const modalContent = (
    <div 
      ref={overlayRef}
      className="fixed inset-0 z-50 flex items-center justify-center p-4 bg-black bg-opacity-50"
      onClick={handleOverlayClick}
      data-testid={testId}
    >
      <div 
        ref={modalRef}
        className={cn(
          'bg-white rounded-lg shadow-xl w-full',
          sizeClasses[size],
          size === 'full' ? 'h-full' : 'max-h-[90vh] overflow-y-auto',
          className
        )}
        role="dialog"
        aria-modal="true"
        aria-labelledby={title ? 'modal-title' : undefined}
      >
        {(title || closable) && (
          <div className="card-header flex items-center justify-between">
            {title && (
              <h2 id="modal-title" className="text-lg font-semibold text-secondary-900">
                {title}
              </h2>
            )}
            {closable && (
              <Button
                variant="ghost"
                size="sm"
                onClick={onClose}
                aria-label="모달 닫기"
                className="p-2"
              >
                <svg 
                  className="w-5 h-5" 
                  fill="none" 
                  stroke="currentColor" 
                  viewBox="0 0 24 24"
                  aria-hidden="true"
                >
                  <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M6 18L18 6M6 6l12 12" />
                </svg>
              </Button>
            )}
          </div>
        )}
        
        <div className="card-body">
          {children}
        </div>
        
        {footer && (
          <div className="card-footer flex justify-end space-x-3">
            {footer}
          </div>
        )}
      </div>
    </div>
  )

  return createPortal(modalContent, document.body)
}
```

### 4. ClassFlow Specialized Components

#### StudentCard 컴포넌트
```typescript
// src/components/classflow/StudentCard.tsx
'use client'

import React, { memo } from 'react'
import { cn } from '@/utils/cn'
import { Card } from '@/components/ui/Card'
import { Badge } from '@/components/ui/Badge'
import type { ClassFlowStudent } from '@/types/app.types'

interface StudentCardProps {
  student: ClassFlowStudent
  isDragging?: boolean
  isSelected?: boolean
  onSelect?: (student: ClassFlowStudent) => void
  onEdit?: (student: ClassFlowStudent) => void
  className?: string
  'data-testid'?: string
}

export const StudentCard = memo<StudentCardProps>(({ 
  student, 
  isDragging = false,
  isSelected = false,
  onSelect,
  onEdit,
  className,
  'data-testid': testId
}) => {
  const statusColors = {
    active: 'success',
    inactive: 'secondary',
    graduated: 'primary',
    transferred: 'warning'
  } as const

  const statusLabels = {
    active: '재학',
    inactive: '휴학',
    graduated: '졸업',
    transferred: '전학'
  }

  function handleClick() {
    if (onSelect) {
      onSelect(student)
    }
  }

  function handleDoubleClick() {
    if (onEdit) {
      onEdit(student)
    }
  }

  function handleKeyDown(event: React.KeyboardEvent) {
    if (event.key === 'Enter' || event.key === ' ') {
      event.preventDefault()
      handleClick()
    } else if (event.key === 'Enter' && event.shiftKey) {
      event.preventDefault()
      handleDoubleClick()
    }
  }

  return (
    <Card
      className={cn(
        'student-card cursor-pointer select-none',
        isDragging && 'dragging',
        isSelected && 'ring-2 ring-primary-500',
        className
      )}
      padding="sm"
      hover={!isDragging}
      onClick={handleClick}
      onDoubleClick={handleDoubleClick}
      onKeyDown={handleKeyDown}
      tabIndex={0}
      role="button"
      aria-label={`학생 ${student.name} 선택`}
      aria-selected={isSelected}
      data-testid={testId}
    >
      <div className="flex items-center justify-between">
        <div className="flex-1 min-w-0">
          <h3 className="font-medium text-secondary-900 truncate">
            {student.name}
          </h3>
          <div className="flex items-center space-x-2 mt-1">
            <Badge 
              variant={statusColors[student.status]}
              size="sm"
            >
              {statusLabels[student.status]}
            </Badge>
            {student.grade && (
              <span className="text-xs text-secondary-500">
                {student.grade}학년
              </span>
            )}
          </div>
        </div>
        
        <div className="flex items-center space-x-1 ml-2">
          {student.tags.map((tag, index) => (
            <Badge 
              key={index}
              variant="secondary"
              size="xs"
            >
              {tag}
            </Badge>
          ))}
        </div>
      </div>
      
      {(student.phone || student.parent_phone) && (
        <div className="mt-2 text-xs text-secondary-500">
          {student.phone && (
            <div>학생: {student.phone}</div>
          )}
          {student.parent_phone && (
            <div>학부모: {student.parent_phone}</div>
          )}
        </div>
      )}
    </Card>
  )
})

StudentCard.displayName = 'StudentCard'
```

#### DropZone 컴포넌트
```typescript
// src/components/classflow/DropZone.tsx
'use client'

import React from 'react'
import { cn } from '@/utils/cn'
import type { BaseComponentProps } from '@/components/ui/types'

interface DropZoneProps extends BaseComponentProps {
  isActive: boolean
  isInvalid?: boolean
  placeholder?: string
  minHeight?: number
  onDrop?: (event: React.DragEvent) => void
  onDragOver?: (event: React.DragEvent) => void
  onDragLeave?: (event: React.DragEvent) => void
}

export function DropZone({
  isActive,
  isInvalid = false,
  placeholder = "학생을 여기에 놓으세요",
  minHeight = 200,
  onDrop,
  onDragOver,
  onDragLeave,
  className,
  children,
  'data-testid': testId
}: DropZoneProps) {
  function handleDragOver(event: React.DragEvent) {
    event.preventDefault()
    event.dataTransfer.dropEffect = isInvalid ? 'none' : 'move'
    onDragOver?.(event)
  }

  function handleDrop(event: React.DragEvent) {
    event.preventDefault()
    if (!isInvalid) {
      onDrop?.(event)
    }
  }

  return (
    <div
      className={cn(
        'drop-zone',
        'flex flex-col items-center justify-center',
        'border-2 border-dashed rounded-lg',
        'transition-all duration-200 ease-in-out',
        isActive && 'drop-zone-active',
        isInvalid && 'drop-zone-invalid',
        className
      )}
      style={{ minHeight }}
      onDrop={handleDrop}
      onDragOver={handleDragOver}
      onDragLeave={onDragLeave}
      role="region"
      aria-label="드롭 영역"
      data-testid={testId}
    >
      {children || (
        <div className="text-center">
          <div className="mb-2">
            <svg 
              className="mx-auto h-12 w-12 text-secondary-400" 
              fill="none" 
              stroke="currentColor" 
              viewBox="0 0 48 48" 
              aria-hidden="true"
            >
              <path 
                strokeLinecap="round" 
                strokeLinejoin="round" 
                strokeWidth={1} 
                d="M34 16l-8-8-8 8m16 0v12a4 4 0 01-4 4H18a4 4 0 01-4-4V16" 
              />
            </svg>
          </div>
          <p className="text-sm text-secondary-600">
            {placeholder}
          </p>
        </div>
      )}
    </div>
  )
}
```

### 5. Data Display Components

#### Table 컴포넌트
```typescript
// src/components/ui/Table.tsx
'use client'

import React from 'react'
import { cn } from '@/utils/cn'
import type { BaseComponentProps } from './types'

interface TableProps extends BaseComponentProps {
  striped?: boolean
  hoverable?: boolean
  bordered?: boolean
  compact?: boolean
}

export function Table({ 
  className, 
  children,
  striped = false,
  hoverable = true,
  bordered = true,
  compact = false,
  'data-testid': testId
}: TableProps) {
  return (
    <div className="overflow-x-auto">
      <table 
        className={cn(
          'min-w-full',
          bordered && 'border border-secondary-200',
          className
        )}
        data-testid={testId}
      >
        {children}
      </table>
    </div>
  )
}

export function TableHeader({ className, children, ...props }: BaseComponentProps) {
  return (
    <thead className={cn('bg-secondary-50', className)} {...props}>
      {children}
    </thead>
  )
}

export function TableBody({ className, children, ...props }: BaseComponentProps) {
  return (
    <tbody className={cn(className)} {...props}>
      {children}
    </tbody>
  )
}

export function TableRow({ 
  className, 
  children, 
  hoverable = true,
  ...props 
}: BaseComponentProps & { hoverable?: boolean }) {
  return (
    <tr 
      className={cn(
        hoverable && 'hover:bg-secondary-50 transition-colors duration-150',
        className
      )} 
      {...props}
    >
      {children}
    </tr>
  )
}

export function TableHead({ className, children, ...props }: BaseComponentProps) {
  return (
    <th 
      className={cn(
        'px-6 py-3 text-left text-xs font-medium text-secondary-500 uppercase tracking-wider',
        className
      )} 
      {...props}
    >
      {children}
    </th>
  )
}

export function TableCell({ className, children, ...props }: BaseComponentProps) {
  return (
    <td 
      className={cn(
        'px-6 py-4 whitespace-nowrap text-sm text-secondary-900',
        className
      )} 
      {...props}
    >
      {children}
    </td>
  )
}
```

#### Badge 컴포넌트
```typescript
// src/components/ui/Badge.tsx
'use client'

import React from 'react'
import { cn } from '@/utils/cn'
import type { BaseComponentProps, ComponentVariant, ComponentSize } from './types'

interface BadgeProps extends BaseComponentProps {
  variant?: ComponentVariant
  size?: ComponentSize
  removable?: boolean
  onRemove?: () => void
}

export function Badge({ 
  className, 
  children,
  variant = 'secondary',
  size = 'sm',
  removable = false,
  onRemove,
  'data-testid': testId
}: BadgeProps) {
  const baseClasses = 'badge'
  const variantClasses = {
    primary: 'badge-primary',
    secondary: 'badge-secondary',
    success: 'badge-success',
    warning: 'badge-warning',
    error: 'badge-error',
    ghost: 'bg-transparent text-secondary-600 border border-secondary-300'
  }
  
  const sizeClasses = {
    xs: 'px-1.5 py-0.5 text-xs',
    sm: 'px-2 py-1 text-xs',
    md: 'px-2.5 py-1 text-sm',
    lg: 'px-3 py-1.5 text-sm',
    xl: 'px-4 py-2 text-base'
  }

  return (
    <span 
      className={cn(
        baseClasses,
        variantClasses[variant],
        sizeClasses[size],
        removable && 'pr-1',
        className
      )}
      data-testid={testId}
    >
      {children}
      {removable && (
        <button
          type="button"
          className="ml-1 -mr-1 p-0.5 rounded-full hover:bg-black hover:bg-opacity-10 focus:outline-none focus:ring-1 focus:ring-current"
          onClick={onRemove}
          aria-label="제거"
        >
          <svg 
            className="w-3 h-3" 
            fill="none" 
            stroke="currentColor" 
            viewBox="0 0 24 24"
            aria-hidden="true"
          >
            <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M6 18L18 6M6 6l12 12" />
          </svg>
        </button>
      )}
    </span>
  )
}
```

### 6. Utility Components & Hooks

#### Loading 컴포넌트
```typescript
// src/components/ui/Loading.tsx
'use client'

import React from 'react'
import { cn } from '@/utils/cn'
import type { BaseComponentProps, ComponentSize } from './types'

interface LoadingProps extends BaseComponentProps {
  size?: ComponentSize
  overlay?: boolean
  text?: string
}

export function Loading({ 
  className,
  size = 'md',
  overlay = false,
  text,
  'data-testid': testId
}: LoadingProps) {
  const sizeClasses = {
    xs: 'w-4 h-4',
    sm: 'w-6 h-6',
    md: 'w-8 h-8',
    lg: 'w-12 h-12',
    xl: 'w-16 h-16'
  }

  const spinner = (
    <div className="flex flex-col items-center justify-center space-y-2">
      <div 
        className={cn(
          'loading-spinner border-primary-600',
          sizeClasses[size],
          className
        )}
        data-testid={testId}
      />
      {text && (
        <p className="text-sm text-secondary-600">{text}</p>
      )}
    </div>
  )

  if (overlay) {
    return (
      <div className="fixed inset-0 z-50 flex items-center justify-center bg-black bg-opacity-50">
        {spinner}
      </div>
    )
  }

  return spinner
}

// Skeleton Loading 컴포넌트
export function Skeleton({ 
  className, 
  width = '100%',
  height = '1rem',
  ...props 
}: BaseComponentProps & { width?: string | number; height?: string | number }) {
  return (
    <div 
      className={cn(
        'animate-pulse bg-secondary-200 rounded',
        className
      )}
      style={{ width, height }}
      {...props}
    />
  )
}
```

### 7. Storybook Setup

#### 스토리북 설정
```typescript
// .storybook/main.ts
import type { StorybookConfig } from '@storybook/nextjs'

const config: StorybookConfig = {
  stories: ['../src/**/*.stories.@(js|jsx|ts|tsx|mdx)'],
  addons: [
    '@storybook/addon-essentials',
    '@storybook/addon-interactions',
    '@storybook/addon-a11y',
    '@storybook/addon-controls',
    '@storybook/addon-viewport'
  ],
  framework: {
    name: '@storybook/nextjs',
    options: {}
  },
  typescript: {
    check: false,
    reactDocgen: 'react-docgen-typescript'
  }
}

export default config

// Button 스토리 예시
// src/components/ui/Button.stories.tsx
import type { Meta, StoryObj } from '@storybook/react'
import { Button } from './Button'
import { PlusIcon, TrashIcon } from '@heroicons/react/24/outline'

const meta: Meta<typeof Button> = {
  title: 'UI/Button',
  component: Button,
  parameters: {
    layout: 'centered',
    docs: {
      description: {
        component: '재사용 가능한 버튼 컴포넌트입니다. 다양한 variant와 size를 지원하며, 권한 기반 렌더링을 포함합니다.'
      }
    }
  },
  tags: ['autodocs'],
  argTypes: {
    variant: {
      control: 'select',
      options: ['primary', 'secondary', 'success', 'warning', 'error', 'ghost']
    },
    size: {
      control: 'select',
      options: ['xs', 'sm', 'md', 'lg', 'xl']
    },
    loading: { control: 'boolean' },
    disabled: { control: 'boolean' },
    fullWidth: { control: 'boolean' }
  }
}

export default meta
type Story = StoryObj<typeof meta>

export const Default: Story = {
  args: {
    children: '기본 버튼'
  }
}

export const Variants: Story = {
  render: () => (
    <div className="flex space-x-4">
      <Button variant="primary">Primary</Button>
      <Button variant="secondary">Secondary</Button>
      <Button variant="success">Success</Button>
      <Button variant="warning">Warning</Button>
      <Button variant="error">Error</Button>
      <Button variant="ghost">Ghost</Button>
    </div>
  )
}

export const WithIcons: Story = {
  args: {
    children: '학생 추가',
    leftIcon: <PlusIcon className="w-4 h-4" />
  }
}

export const Loading: Story = {
  args: {
    children: '저장 중...',
    loading: true
  }
}

export const Disabled: Story = {
  args: {
    children: '비활성화',
    disabled: true
  }
}
```

## 🔧 Implementation Steps

### Phase 1: Core Components (Day 1-2)
1. [ ] Button, Input, Card 기본 컴포넌트
2. [ ] TypeScript 타입 시스템 구축
3. [ ] 접근성 기본 구현
4. [ ] 테마 시스템 연동

### Phase 2: Layout & Navigation (Day 2-3)
1. [ ] Modal, Table, Badge 컴포넌트
2. [ ] 권한 기반 렌더링 통합
3. [ ] 반응형 디자인 적용
4. [ ] 키보드 네비게이션

### Phase 3: ClassFlow Components (Day 3)
1. [ ] StudentCard, DropZone 특화 컴포넌트
2. [ ] 드래그앤드롭 최적화
3. [ ] 60fps 성능 보장
4. [ ] 가상화 지원

### Phase 4: Documentation & Testing (Day 4)
1. [ ] Storybook 설정 및 문서화
2. [ ] 접근성 테스트 (axe-core)
3. [ ] 성능 테스트
4. [ ] 컴포넌트 단위 테스트

## 🧪 Testing Strategy

### 컴포넌트 테스트
```typescript
// src/components/ui/__tests__/Button.test.tsx
import { render, screen, fireEvent } from '@testing-library/react'
import { describe, test, expect, vi } from 'vitest'
import { Button } from '../Button'
import { AuthProvider } from '@/contexts/AuthContext'

const MockAuthProvider = ({ children, hasPermission = true }) => (
  <AuthProvider value={{ hasPermission: () => hasPermission }}>
    {children}
  </AuthProvider>
)

describe('Button Component', () => {
  test('renders correctly', () => {
    render(
      <MockAuthProvider>
        <Button>테스트 버튼</Button>
      </MockAuthProvider>
    )
    
    expect(screen.getByRole('button')).toBeInTheDocument()
    expect(screen.getByText('테스트 버튼')).toBeInTheDocument()
  })

  test('handles click events', () => {
    const handleClick = vi.fn()
    
    render(
      <MockAuthProvider>
        <Button onClick={handleClick}>클릭</Button>
      </MockAuthProvider>
    )
    
    fireEvent.click(screen.getByRole('button'))
    expect(handleClick).toHaveBeenCalledTimes(1)
  })

  test('respects permission system', () => {
    render(
      <MockAuthProvider hasPermission={false}>
        <Button permission={{ resource: 'students', action: 'write' }}>
          권한 필요
        </Button>
      </MockAuthProvider>
    )
    
    expect(screen.queryByRole('button')).not.toBeInTheDocument()
  })

  test('shows loading state', () => {
    render(
      <MockAuthProvider>
        <Button loading>로딩</Button>
      </MockAuthProvider>
    )
    
    expect(screen.getByText('로딩')).toBeInTheDocument()
    expect(document.querySelector('.loading-spinner')).toBeInTheDocument()
  })

  test('is accessible', () => {
    render(
      <MockAuthProvider>
        <Button aria-label="접근성 테스트">버튼</Button>
      </MockAuthProvider>
    )
    
    const button = screen.getByRole('button')
    expect(button).toHaveAttribute('aria-label', '접근성 테스트')
  })
})
```

### 접근성 테스트
```typescript
// src/__tests__/accessibility.test.tsx
import { render } from '@testing-library/react'
import { axe, toHaveNoViolations } from 'jest-axe'
import { Button, Input, Modal } from '@/components/ui'

expect.extend(toHaveNoViolations)

describe('Accessibility Tests', () => {
  test('Button has no accessibility violations', async () => {
    const { container } = render(<Button>테스트 버튼</Button>)
    const results = await axe(container)
    expect(results).toHaveNoViolations()
  })

  test('Input has no accessibility violations', async () => {
    const { container } = render(
      <Input 
        label="테스트 입력" 
        id="test-input"
        aria-describedby="test-hint"
      />
    )
    const results = await axe(container)
    expect(results).toHaveNoViolations()
  })

  test('Modal has no accessibility violations', async () => {
    const { container } = render(
      <Modal isOpen title="테스트 모달" onClose={() => {}}>
        모달 내용
      </Modal>
    )
    const results = await axe(container)
    expect(results).toHaveNoViolations()
  })
})
```

## 📊 Performance Benchmarks

- 컴포넌트 렌더링: < 16ms (60fps)
- 번들 크기: < 50KB per component
- 메모리 사용량: < 5MB peak
- First Paint: < 100ms
- ClassFlow 드래그앤드롭: 60fps 보장

## 🚀 Expected Outcomes

1. **재사용 가능한 컴포넌트**: 25+ 검증된 컴포넌트
2. **접근성 완전 준수**: WCAG 2.1 AA 100% 달성
3. **개발 생산성**: 스토리북으로 50% 빠른 UI 개발
4. **타입 안전성**: TypeScript Generic으로 런타임 에러 제거
5. **성능 최적화**: ClassFlow 60fps 드래그앤드롭

## 📝 Documentation

- [ ] 컴포넌트 API 문서
- [ ] 디자인 시스템 가이드
- [ ] 접근성 체크리스트
- [ ] 성능 최적화 가이드
- [ ] Storybook 인터랙티브 문서

## 🔗 Related Tasks

- **Next**: T-007 (API 엔드포인트 구현)
- **Depends**: T-002 (디자인 시스템), T-004 (TypeScript), T-005 (인증)
- **Related**: T-008 (ClassFlow 구현), T-009 (학생 관리)

---

**Status**: TODO  
**Assignee**: Developer  
**Created**: 2025-08-10  
**Updated**: 2025-08-10  
**Completion**: 0%